# Required: HTTP Basic Auth for OpenCode Web.
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD=replace-with-a-long-unique-password

# Optional: add only the provider API keys you actually use in EasyPanel.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
